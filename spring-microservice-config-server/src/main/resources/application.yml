server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nguleduy/microservice.git/
          search-paths:
            - fornecedor
            - joseph
            - transportation
